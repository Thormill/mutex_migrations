source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in mutex_migrations.gemspec
gemspec do |spec|
  spec.add_runtime_dependency 'rails', '~> 6.0', '>= 6.0'
  end

